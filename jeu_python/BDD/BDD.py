import sqlite3
import sys

class BDD():
    def __init__(self):
        self.conn = sqlite3.connect('test_database')
        if base is None:
            #créer la base
            pass
        else:
            #ce connecter à la base